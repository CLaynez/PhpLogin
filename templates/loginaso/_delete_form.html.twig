<form method="post" action="{{ path('app_loginaso_delete', {'id': loginaso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ loginaso.id) }}">
    <button class="btn">Delete</button>
</form>
